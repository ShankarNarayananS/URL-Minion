<!-- Here this is html like file which is used to render the page as we
had put in res.render('home') in index.js
Which means JS can now render full HTML pages stored locally instead of creating
them dynamically. 


In bootstrap,
"col-lg-12":col means column,lg means larger screen,12 is dimension


-->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h1>URL Shortener</h1>
            </div>
        </div>
        <form action="/create" method="POST">
            <div class="row">
                <div class="input-group mb-3">
                    <input type="text" name="longurl" class="form-control" placeholder="Paste your long URL"
                        aria-label="Recipient's username" aria-describedby="button-addon2">
                    <div class="input-group-append">
                        <input type="submit" class="btn btn-outline-secondary" value="Submit">
                    </div>
                </div>
            </div>
        </form>

        <div class="row">
            <% for(let i=0;i< urlResult.length;i++) { %>
            <div class="col-lg-3">
                <div class="card border-secondary mb-3" style="max-width: 18rem;">
                    <div class="card-header">Total Click : <%= urlResult[i].clickCount %></div>
                    <div class="card-body text-secondary">
                        <h6 class="card-title">
                            <a href="http://localhost:3000/<%= urlResult[i].shortUrl %>" target="_blank">http://localhost:3000/<%= urlResult[i].shortUrl %></a>
                        </h6>
                        <p class="card-text"><%= urlResult[i].longUrl %></p>
                        <a href="/delete/<%= urlResult[i].id %>" class="btn btn-sm btn-danger">Remove</a>
                    </div>
                </div>
            </div>
            <% } %>
        </div>
    </div>
</body>

</html>